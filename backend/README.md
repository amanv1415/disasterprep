# Bandhu Backend (placeholder)

This folder contains a minimal Express backend scaffold.

To run locally:

```sh
cd backend
npm install
npm run dev
# then open http://localhost:3001/api/health
```

Add routes, middleware and DB config under `src/` as needed.
